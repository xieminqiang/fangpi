
name: Deploy Prod

on:
  push:
    branches:
      - main

concurrency:
  group: deploy-prod
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24"

      - name: Install frontend dependencies
        working-directory: web
        run: npm install

      - name: Build frontend
        working-directory: web
        run: npm run build

      - name: Build backend
        working-directory: server
        run: |
          go mod tidy
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o server .

      - name: Verify build
        run: |
          test -d web/dist
          test -f web/dist/index.html
          test -f server/server

      - name: Upload frontend
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.KEY }}
          port: 22
          source: "web/dist/*"
          target: "/opt/gva/dist"
          strip_components: 2

      - name: Upload backend
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.KEY }}
          port: 22
          source: "server/server"
          target: "/opt/gva"
          strip_components: 1

      - name: Upload resource
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.KEY }}
          port: 22
          source: "server/resource/*"
          target: "/opt/gva/resource"
          strip_components: 2

      - name: Upload restart script
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.KEY }}
          port: 22
          source: "deploy/scripts/restart.sh"
          target: "/opt/gva"
          strip_components: 2

<<<<<<< HEAD
      - name: Restart service
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USER }}
          key: ${{ secrets.KEY }}
          port: 22
          script: |
            chmod +x /opt/gva/server
            chmod +x /opt/gva/restart.sh
            cd /opt/gva
            bash restart.sh

=======
          for name in SSH_HOST SSH_USER; do
            if [ -z "${!name:-}" ]; then
              echo "::error::GitHub Secret is empty: $name"
              exit 1
            fi
          done

          SSH_PORT="${SSH_PORT:-22}"
          DEPLOY_DIR="${DEPLOY_DIR:-/opt/gva/}"
          case "$DEPLOY_DIR" in
            */) ;;
            *) DEPLOY_DIR="${DEPLOY_DIR}/" ;;
          esac

          mkdir -p ~/.ssh
          ssh-keyscan -H -p "$SSH_PORT" "$SSH_HOST" >> ~/.ssh/known_hosts 2>/dev/null || true

          REMOTE="${SSH_USER}@${SSH_HOST}"
          SSH_OPTS="-p $SSH_PORT -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15"
          SCP_OPTS="-P $SSH_PORT -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o StrictHostKeyChecking=accept-new -o ConnectTimeout=15"

          echo "Deploy target: ${SSH_USER}@${SSH_HOST}:${SSH_PORT} -> ${DEPLOY_DIR}"
          echo "Testing SSH connection to ${SSH_HOST}:${SSH_PORT}..."
          ssh $SSH_OPTS "$REMOTE" "echo connected && mkdir -p ${DEPLOY_DIR}dist ${DEPLOY_DIR}resource"

          echo "Uploading frontend..."
          scp $SCP_OPTS -r web/dist/. "${REMOTE}:${DEPLOY_DIR}dist/"

          echo "Uploading backend binary..."
          scp $SCP_OPTS server/server "${REMOTE}:${DEPLOY_DIR}server"

          echo "Uploading backend resource..."
          scp $SCP_OPTS -r server/resource/. "${REMOTE}:${DEPLOY_DIR}resource/"

          echo "Uploading restart script..."
          scp $SCP_OPTS deploy/scripts/restart.sh "${REMOTE}:${DEPLOY_DIR}restart.sh"

          echo "Restarting service..."
          ssh $SSH_OPTS "$REMOTE" "chmod +x ${DEPLOY_DIR}restart.sh ${DEPLOY_DIR}server && cd ${DEPLOY_DIR} && bash restart.sh"

          echo "Deploy success"
>>>>>>> 027820c03fb3a377b733a389f49f83ed8f71ef97
